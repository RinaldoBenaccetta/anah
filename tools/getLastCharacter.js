'use strict'

/**
 * Return the last character of a string.
 *
 * @param {string} string
 */
const getLastCharacter = (string) => {
  return string.slice(string.length - 1)
}

module.exports = getLastCharacter
